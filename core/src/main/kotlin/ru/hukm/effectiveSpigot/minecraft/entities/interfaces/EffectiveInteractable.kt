package ru.hukm.effectiveSpigot.minecraft.entities.interfaces

import org.bukkit.entity.EntityType

interface EffectiveInteractable {
    data class Data(
        val entityType: EntityType,
        val click:
    )

    companion object {
        val namespacedKeyToInteractCallbaback =
    }
}